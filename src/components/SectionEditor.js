// src/components/SectionEditor.js
// Only used if you later want a rich‐text editor for custom sections;
// for this layout all editing happens inline in Sidebar, so you can leave this empty.
import React from 'react';
export default function SectionEditor() { return null; }
