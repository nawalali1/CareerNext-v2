// src/components/SectionEditor.js
import React from 'react';
import PropTypes from 'prop-types';

export default function SectionEditor({ /* unused now */ }) {
  return null;
}

SectionEditor.propTypes = {};
