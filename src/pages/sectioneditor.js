// src/components/SectionEditor.js
// (now only needed if you want rich‐text or custom editing;
// you can omit this if you’re happy with the Sidebar inputs.)
import React from 'react';
import PropTypes from 'prop-types';

export default function SectionEditor({ /* unused now */ }) {
  return null;
}

SectionEditor.propTypes = {};
